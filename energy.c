#include <stdio.h>
#include <stdlib.h>

float energy(float* r, float* v, float* m)
{
	int E = 4;
    int numParticles = sizeof(r) / sizeof(float);    

    float* T = (float*)malloc(numParticles * sizeof(float));

    free(T);
    
    return E;
}
